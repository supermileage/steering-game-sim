{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Game",
            "program": "${workspaceFolder}/game",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}